# Import input file
with open('./depths.txt') as file:
    depths = file.readlines()

# Loop through and counter
counter = 0
for i in range(1:len(depths)):
    curr_depth = depths[i]
    next_depth = depths[i+1]
    if curr_depth - next_depth < 0:
        counter = counter + 1

print(counter)    
